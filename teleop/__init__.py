"""
Marks `teleop/` as a Python package so `from teleop.env import FrankaMujocoEnv`
works from record_episode.py in the parent folder.

Leave this file empty (this docstring is optional). If you delete it, imports
break on some Python setups — keep it.
"""
